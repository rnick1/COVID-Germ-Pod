<div class="container">
    <div class="card">
        <a class="navbar-brand" href="/group/{{name}}">{{group.name}}</a>
        <div class="card-body">
            <p class="card-text">Here are the rules: {{rule.name}}</p>
            <button class="btn btn-primary" type="submit">Join Pod!</button>
        </div>
    </div>
</div>
</div>