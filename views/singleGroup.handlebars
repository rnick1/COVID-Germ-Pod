<div class="container">
    <div class="card">
        <a class="navbar-brand" href="/group/{{name}}">{{dataValues.name}}</a>
        <div class="card-body">
            {{!-- <p class="card-text">Here are the rules: {{dataValues.rules}}</p> --}}
            <button class="btn btn-primary" type="submit">Join Pod!</button>
        </div>
    </div>
</div>
</div>